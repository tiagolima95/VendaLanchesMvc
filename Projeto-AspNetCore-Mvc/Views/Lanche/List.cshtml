@model IEnumerable<Projeto_AspNetCore_Mvc.Models.Lanche>

<div>
    <h2>Todos os Lanches</h2>
</div>

@foreach(var lanche in Model)
{
    <div>
        <h4>@lanche.Nome</h4>
        <p><img src="@lanche.ImageUrl"></p>
        <h4>Preço: @lanche.Preco.ToString("c")</h4>
    </div>
}